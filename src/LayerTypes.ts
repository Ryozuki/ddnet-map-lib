export enum LayerTypes {
    INVALID = 0,
    GAME,
    TILES,
    QUADS,
    FRONT,
    TELE,
    SPEEDUP,
    SWITCH,
    TUNE,
    SOUNDS_DEPRECATED, // deprecated! do not use this, this is just for compatibility reasons
    SOUNDS,
}
